{{#if packageName}}package {{packageName}};{{/if}}
{{#each imports}}
import {{this}};{{/each}}
{{#if javadoc}}/** {{javadoc}} */{{/if}}
{{#each modifiers}}{{this}} {{/each}}{{type}} {{name}}{{#if baseClass}} extends {{baseClass}}{{/if}}{{#if interfaces}} implements {{#each interfaces}}{{this}}{{#unless @first}}, {{/unless}}{{/each}}{{/if}} {
{{#each fields}}
    {{~> field this}}
{{/each}}
    {{#each constructors}}{{> method this}}
    {{/each}}
    {{#each methods}}
    {{> method this}}
    {{/each}}
}